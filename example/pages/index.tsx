import type { NextPage } from 'next';
import { Button } from 'react-inter-phone-input';

const Home: NextPage = () => {
  return (
    <div>
      <Button text="asd" />
    </div>
  );
};

export default Home;
